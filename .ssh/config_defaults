# Usage: Include .ssh/config_defaults

# Global defaults 
# ssh picks the first option it finds
# so we put these last so they are fallbacks when not specified
Host *
    # Security settings
    IdentitiesOnly yes                    # Only use explicitly specified keys
    PubkeyAuthentication yes              # Enable public key authentication
    PasswordAuthentication no             # Disable password authentication
    ChallengeResponseAuthentication no    # Disable challenge-response auth

    # Connection settings
    ServerAliveInterval 60                # Send keepalive every 60 seconds
    ServerAliveCountMax 3                 # Disconnect after 3 failed keepalives
    ConnectTimeout 30                     # Connection timeout in seconds
    AddKeysToAgent yes                    # Automatically add keys to ssh-agent

    # Protocol settings
    Protocol 2                            # Use SSH protocol version 2
    Compression yes                       # Enable compression
    
    # Host key verification
    StrictHostKeyChecking ask             # Prompt for unknown host keys
    CheckHostIP yes                       # Verify host IP addresses
    
    # Default connection settings
    Port 22                               # Default SSH port
    ForwardAgent no                       # Don't forward ssh-agent by default
    ForwardX11 no                         # Don't forward X11 by default